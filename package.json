{
  "name": "edubridge-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "serve": "npx http-server ./ -p 8080 -c-1",
    "build:esbuild": "esbuild assets/js/main.js --bundle --minify --sourcemap --outdir=public/assets/js",
    "lint": "echo \"Add ESLint config if desired\""
  },
  "devDependencies": {
    "esbuild": "^0.18.0"
  }
}
